generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum OcrStatus {
  not_required
  pending
  running
  succeeded
  failed
}

enum AnalysisStatus {
  pending
  running
  succeeded
  failed
}
